pipeline {
    agent any
    stages {
        stage("clean"){
            steps {
                // clear the repository
                echo "Clearing the repository builds"
            }
        }
    }
}